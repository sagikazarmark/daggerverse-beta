# Code generated by the daggerverse module. DO NOT EDIT.

jobs:
    check:
        name: ${{ inputs.module }}
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v7.0.0
            - name: Check
              uses: dagger/dagger-for-github@v8.4.1
              with:
                commit: 86d1d2f5791bcf3213d56903cfa81a3ba0abe54a
                module: ${{ inputs.module }}/tests
                verb: check
name: Daggerverse Dispatch
"on":
    workflow_dispatch:
        inputs:
            module:
                description: Module to check
                options:
                    - build-environment
                    - bun-environment
                    - cargo-lock
                    - crates
                    - crypto
                    - daggerverse
                    - deno-environment
                    - dioxus
                    - distro
                    - gha
                    - git-daemon
                    - github
                    - helm
                    - hurl
                    - javascript-environment
                    - k3s
                    - kubectl
                    - minisign
                    - node-environment
                    - olareg
                    - overlays
                    - php-dist
                    - php-ext-rs
                    - registry-config
                    - restate
                    - rust
                    - rust-environment
                    - wasm-tools
                    - worker-build
                    - wrangler
                    - zig-environment
                required: true
                type: choice
permissions:
    contents: read
