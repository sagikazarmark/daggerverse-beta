# Code generated by Dagger. DO NOT EDIT.

jobs:
    check:
        name: ${{ matrix.env }} / ${{ matrix.check }}
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
            - name: Check
              with:
                args: --env ${{ matrix.env }} ${{ matrix.check }}
                verb: check
        strategy:
            fail-fast: false
            matrix:
                check:
                    - alpha:build
                    - alpha:fmt
                    - alpha:test:integration
                    - alpha:test:unit
                    - beta:lint
                env:
                    - ci
                    - nightly
name: Dagger
"on":
    pull_request: {}
    push:
        branches:
            - main
permissions:
    contents: read
