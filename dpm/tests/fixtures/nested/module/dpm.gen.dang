# Code generated by dpm. DO NOT EDIT.

type Strings {
  slug(s: String!): String! {
    s.trimSpace.toLower.replace(" ", "-")
  }

  truncate(s: String!, maxLength: Int!, omission: String! = "..."): String! {
    if (maxLength <= 0) {
      return ""
    }

    let chars = s.split("")
    if (chars.length <= maxLength) {
      return s
    }

    let omissionChars = omission.split("")
    if (omissionChars.length >= maxLength) {
      return omissionChars.takeFirst(maxLength).join("")
    }

    chars.takeFirst(maxLength - omissionChars.length).join("") + omission
  }
}
