# Code generated by dpm. DO NOT EDIT.

type Strings {
  slug(s: String!): String! {
    s.trimSpace.toLower.replace(" ", "-")
  }

  truncate(s: String!, maxLength: Int!): String! {
    if (maxLength <= 0) {
      return ""
    }

    let chars = s.split("")
    if (chars.length <= maxLength) {
      return s
    }

    let ellipsis = "..."
    let ellipsisChars = ellipsis.split("")
    if (ellipsisChars.length >= maxLength) {
      return ellipsisChars.takeFirst(maxLength).join("")
    }

    chars.takeFirst(maxLength - ellipsisChars.length).join("") + ellipsis
  }
}
